# TEST_EVIDENCE

개인 빌드 환경에서 실제 실행한 시험 목록. 경로는 `<PROJECT_ROOT>`/`<TEST_ROOT>`/`<HOME>` 으로 정규화했고, 원시 로그(개인 절대 경로 포함)는 `test-evidence/raw/` 에 로컬 보관하며 반입 ZIP 에 포함하지 않습니다. 실패/미실행은 그대로 기록합니다.

| 이름 | 상태 | exit | 소요(s) | 환경 | 명령 |
|---|---|---|---|---|---|
| host-01-pytest-all | PASS | 0 | 101.26 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m pytest -q tests -p no:cacheprovider` |
| host-02-ruff-check | PASS | 0 | 0.03 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m ruff check .` |
| host-03-ruff-format | PASS | 0 | 0.03 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m ruff format --check .` |
| host-04-mypy | PASS | 0 | 1.42 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m mypy corp_dl_agent` |
| host-05-build-wheel | PASS | 0 | 0.72 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m build --wheel --no-isolation -o dist` |
| host-06-demo-offline-cpu | PASS | 0 | 7.46 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m corp_dl_agent demo --offline --device cpu --output-dir workspace/demo-evidence --json` |
| host-07-selftest | PASS | 0 | 3.17 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m corp_dl_agent self-test --set paths.data_root=workspace/selftest-ws` |
| host-08-package-build-win | PASS | 0 | 4.54 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m corp_dl_agent package build --profile win-x64-cp312-cpu --wheelhouse wheelhouse/win-x64-cp312-cpu --app-wheel dist/co...` |
| host-09-package-build-linux | PASS | 0 | 56.52 | Linux x86_64 / CPython-3.12.3-(.venv) | `.venv/bin/python -m corp_dl_agent package build --profile linux-x64-cp312-cpu --wheelhouse wheelhouse/linux-x64-cp312-cpu --app-wheel dis...` |
| host-10-verify-win-zip-static | PASS | 0 | 0.77 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 표준 라이브러리 verifier) | `/usr/bin/python3.12 scripts/verify.py --package dist/DIA_4.0.0_win-x64-cp312-cpu.zip --target-profile /tmp/claude-0/-home-user/bb3392bd-2...` |
| host-11-verify-linux-zip | PASS | 0 | 9.41 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 표준 라이브러리 verifier) | `/usr/bin/python3.12 scripts/verify.py --package dist/DIA_4.0.0_linux-x64-cp312-cpu.zip --json` |
| transfer-00-sha256 | PASS | 0 | 8.33 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-00-sha256.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-00-sha256.sh"'` |
| transfer-01-extract | PASS | 0 | 3.48 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-01-extract.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-01-extract.sh"'` |
| transfer-02-preflight | PASS | 0 | 2.01 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-02-preflight.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-02-preflight.sh"'` |
| transfer-03-verify | PASS | 0 | 9.39 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-03-verify.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-03-verify.sh"'` |
| transfer-03b-verify-tampered-must-fail | PASS | 0 | 0.18 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-03b-verify-tampered-must-fail.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-03b-verify-tampered-must-fail.sh"'` |
| transfer-04-install | PASS | 0 | 107.32 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-04-install.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-04-install.sh"'` |
| transfer-05-active | PASS | 0 | 0.1 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-05-active.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-05-active.sh"'` |
| transfer-06-selftest | PASS | 0 | 3.38 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-06-selftest.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-06-selftest.sh"'` |
| transfer-07-doctor | PASS | 0 | 3.29 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-07-doctor.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-07-doctor.sh"'` |
| transfer-08-demo | PASS | 0 | 7.87 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-08-demo.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-08-demo.sh"'` |
| transfer-09-install-tampered-keeps-active | PASS | 0 | 0.21 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-09-install-tampered-keeps-active.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-09-install-tampered-keeps-active.sh"'` |
| transfer-10-no-pip-index | PASS | 0 | 0.1 | Linux x86_64 / CPython 3.12.3 (/usr/bin/python3.12, 회사 승인 Python 역할) / unshare -n (network namespace, external interfaces absent, loopback only) / non-root user 'tester' (no sudo), cwd=/tmp, Korean+space install path, step script: steps/transfer-10-no-pip-index.sh | `unshare -n bash -c ip link set lo up 2>/dev/null; su - tester -c 'bash "<TEST_ROOT>/steps/transfer-10-no-pip-index.sh"'` |
| None | None | None | None |   /  | `` |

각 항목의 stdout/stderr 꼬리와 lock hash 는 `test-evidence/<이름>.json` 에 있습니다.
