# config-examples

- `company_config.example.yaml`: 회사 설정 예시. secret 값은 넣지 않는다 (`env:` / `file:` 참조만).
- `cad_field_mapping.example.yaml`: CATIA export 열 이름 매핑 예시.
- 사내에서 `company/config/` 에 복사하여 채운다. 업데이트 시 이 예시가 회사 설정을 덮어쓰지 않는다.
